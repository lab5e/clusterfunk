// Package fsmtool contains tools for creating FSMs, particularly transitioning
// between states. State machines in Go is quite simple so there's no need for
// a library that handles *everything*.
package fsmtool
